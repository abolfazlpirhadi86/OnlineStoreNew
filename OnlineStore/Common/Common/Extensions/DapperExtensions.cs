using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Common.Extensions
{
    public static class DapperExtensions
    {
        //public static TEntity Insert<TEntity>(this IDbConnection connection, string tableName, dynamic param)
        //{
            //IEnumerable<TEntity> result = SqlMapper.Query<TEntity>(connection, DynamicQuery.GetInsertQuery(tableName, param), param);
            //return result.First();
        //}
    }
}
